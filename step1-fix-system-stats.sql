-- مرحله 1: اصلاح مشکل System Stats API
-- جدول users ستون is_active نداره، بلکه status داره

-- هیچ تغییری در دیتابیس نیاز نیست
-- فقط کد API باید اصلاح شه

SELECT 'مرحله 1: جداول موجود درست هستند، فقط کد API نیاز به اصلاح دارد' as message;